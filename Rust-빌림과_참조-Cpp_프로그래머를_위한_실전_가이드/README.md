# Rust 빌림과 참조: C++ 프로그래머를 위한 실전 가이드  
  
저자: 최흥배, Claude AI   
     
-----    

## 목차

### 1장. 기초 개념 훑어보기
1.1 소유권의 기본 원칙
1.2 빌림과 참조의 핵심 규칙
1.3 C++의 참조/포인터와 Rust의 차이점
1.4 간단한 예제로 보는 빌림 검사기

### 2장. 구조체와 메서드에서의 빌림 문제
2.1 self, &self, &mut self의 선택 기준
2.2 메서드 체이닝과 가변 빌림 충돌
2.3 여러 메서드를 동시에 호출할 때의 문제
2.4 내부 가변성이 필요한 순간

### 3장. 멤버 변수 접근 패턴
3.1 필드 빌림과 구조체 전체 빌림의 충돌
3.2 여러 필드를 동시에 접근하기
3.3 메서드 내에서 다른 메서드 호출 시 발생하는 문제
3.4 분할 빌림(Split Borrowing) 패턴

### 4장. 컬렉션과 반복자에서의 빌림
4.1 Vec 순회 중 수정이 필요할 때
4.2 HashMap/BTreeMap 사용 시 빌림 문제
4.3 반복자를 사용하면서 컬렉션 수정하기
4.4 인덱스 기반 접근 vs 반복자

### 5장. 객체 간 참조 관계 설계
5.1 상호 참조하는 객체들 (양방향 참조)
5.2 부모-자식 관계의 구현
5.3 그래프 구조 표현하기
5.4 Rc와 RefCell을 사용한 해결책
5.5 Weak 참조로 순환 참조 방지하기

### 6장. 라이프타임과 복잡한 빌림
6.1 구조체에 참조를 저장할 때
6.2 여러 라이프타임이 얽힌 상황
6.3 메서드 반환값의 라이프타임
6.4 라이프타임 엘리전 규칙 활용

### 7장. 내부 가변성 패턴
7.1 Cell과 RefCell의 차이와 사용법
7.2 Mutex와 RwLock으로 스레드 안전성 확보
7.3 내부 가변성의 런타임 비용
7.4 언제 내부 가변성을 선택해야 하는가

### 8장. 실전 리팩토링 사례
8.1 C++ 스타일 클래스를 Rust로 변환하기
8.2 게터/세터 패턴 재고하기
8.3 빌더 패턴과 빌림
8.4 옵저버 패턴 구현하기
8.5 상태 기계 패턴과 소유권 이동

### 9장. 고급 해결 기법
9.1 인덱스 타입으로 간접 참조하기
9.2 아레나 할당자 패턴
9.3 슬롯맵과 세대별 인덱스
9.4 unsafe를 최소화하면서 복잡한 구조 만들기

### 10장. 실전 프로젝트: GUI 애플리케이션
10.1 위젯 트리 구조 설계
10.2 이벤트 핸들러와 콜백
10.3 상태 관리와 UI 업데이트
10.4 전체 아키텍처 리뷰

### 부록
A. 빌림 검사기 에러 메시지 해석 가이드
B. 자주 마주치는 패턴과 해결책 치트시트
C. C++ 디자인 패턴의 Rust 대응표     