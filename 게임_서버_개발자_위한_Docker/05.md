# 게임 서버 개발자를 위한 Docker  

저자: 최흥배, AI-Assisted   
    
권장 개발 환경
- **OS**: Windows 11 이상, WSL2 

-----    
  
# 5장. 소켓 게임 서버 Docker화

이 장에서는 TCP 소켓 기반 게임 서버를 만들고 Docker 컨테이너로 실행하는 방법을 다룬다. REST API와 달리 소켓 서버는 클라이언트와 지속적인 연결을 유지하므로 네트워크 설정과 포트 관리에 더 세심한 주의가 필요하다.

## 5.1 TCP 소켓 게임 서버 만들기

실시간 멀티플레이어 게임에서는 HTTP보다 TCP 소켓을 사용하여 낮은 지연시간과 지속적인 연결을 유지한다. 간단한 채팅 기능이 있는 게임 서버를 만들어본다.

### 프로젝트 생성

WSL 터미널에서 새 콘솔 프로젝트를 생성한다.

```bash
mkdir SocketGameServer
cd SocketGameServer
dotnet new console -n GameSocketServer
cd GameSocketServer
```

### 소켓 서버 아키텍처

TCP 소켓 서버의 동작 방식을 다이어그램으로 표현하면 다음과 같다.

```
┌─────────────────────────────────────────┐
│         Socket Game Server              │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │   TcpListener (Port 9000)         │  │
│  │   - Accept connections            │  │
│  └──────────────┬────────────────────┘  │
│                 │                       │
│                 ▼                       │
│  ┌───────────────────────────────────┐  │
│  │   Connected Clients Manager       │  │
│  │   - Track active clients          │  │
│  │   - Broadcast messages            │  │
│  └──────────────┬────────────────────┘  │
│                 │                       │
│        ┌────────┴────────┐              │
│        ▼                 ▼              │
│  ┌──────────┐      ┌──────────┐         │
│  │ Client 1 │      │ Client 2 │         │
│  │ Handler  │      │ Handler  │  ...    │
│  └──────────┘      └──────────┘         │
└─────────────────────────────────────────┘
         ▲                    ▲
         │                    │
    ┌────┴─────┐         ┌────┴─────┐
    │ Client 1 │         │ Client 2 │
    │ (Game)   │         │ (Game)   │
    └──────────┘         └──────────┘
```

### 패킷 프로토콜 정의

먼저 클라이언트와 서버 간 통신에 사용할 간단한 패킷 구조를 정의한다.

**Models/GamePacket.cs** 파일을 생성한다.

```csharp
using System.Text.Json;

namespace GameSocketServer.Models
{
    public enum PacketType
    {
        Login = 1,
        Logout = 2,
        Chat = 3,
        Move = 4,
        Heartbeat = 5
    }

    public class GamePacket
    {
        public PacketType Type { get; set; }
        public string PlayerId { get; set; } = string.Empty;
        public string Data { get; set; } = string.Empty;
        public long Timestamp { get; set; }

        public byte[] Serialize()
        {
            var json = JsonSerializer.Serialize(this);
            var bytes = System.Text.Encoding.UTF8.GetBytes(json);
            var length = BitConverter.GetBytes(bytes.Length);
            
            // 4바이트 길이 + JSON 데이터
            var packet = new byte[4 + bytes.Length];
            Array.Copy(length, 0, packet, 0, 4);
            Array.Copy(bytes, 0, packet, 4, bytes.Length);
            
            return packet;
        }

        public static GamePacket? Deserialize(byte[] data)
        {
            var json = System.Text.Encoding.UTF8.GetString(data);
            return JsonSerializer.Deserialize<GamePacket>(json);
        }
    }
}
```

### 클라이언트 세션 관리

**Models/ClientSession.cs** 파일을 생성하여 각 클라이언트 연결을 관리한다.

```csharp
using System.Net.Sockets;

namespace GameSocketServer.Models
{
    public class ClientSession
    {
        public string SessionId { get; set; } = Guid.NewGuid().ToString();
        public string PlayerId { get; set; } = string.Empty;
        public TcpClient Client { get; set; }
        public NetworkStream Stream { get; set; }
        public DateTime ConnectedAt { get; set; }
        public DateTime LastHeartbeat { get; set; }

        public ClientSession(TcpClient client)
        {
            Client = client;
            Stream = client.GetStream();
            ConnectedAt = DateTime.UtcNow;
            LastHeartbeat = DateTime.UtcNow;
        }

        public async Task SendPacketAsync(GamePacket packet)
        {
            var data = packet.Serialize();
            await Stream.WriteAsync(data, 0, data.Length);
        }

        public async Task<GamePacket?> ReceivePacketAsync()
        {
            try
            {
                // 4바이트 길이 읽기
                var lengthBuffer = new byte[4];
                var bytesRead = await Stream.ReadAsync(lengthBuffer, 0, 4);
                
                if (bytesRead == 0) return null;
                
                var length = BitConverter.ToInt32(lengthBuffer, 0);
                
                // 실제 데이터 읽기
                var dataBuffer = new byte[length];
                var totalRead = 0;
                
                while (totalRead < length)
                {
                    bytesRead = await Stream.ReadAsync(dataBuffer, totalRead, length - totalRead);
                    if (bytesRead == 0) return null;
                    totalRead += bytesRead;
                }
                
                return GamePacket.Deserialize(dataBuffer);
            }
            catch
            {
                return null;
            }
        }
    }
}
```

### 게임 서버 메인 로직

**Services/GameServer.cs** 파일을 생성하여 서버의 핵심 로직을 구현한다.

```csharp
using System.Net;
using System.Net.Sockets;
using GameSocketServer.Models;

namespace GameSocketServer.Services
{
    public class GameServer
    {
        private readonly int _port;
        private TcpListener? _listener;
        private readonly Dictionary<string, ClientSession> _sessions = new();
        private readonly object _lock = new();
        private CancellationTokenSource? _cts;

        public GameServer(int port)
        {
            _port = port;
        }

        public async Task StartAsync()
        {
            _listener = new TcpListener(IPAddress.Any, _port);
            _listener.Start();
            _cts = new CancellationTokenSource();

            Console.WriteLine($"[Server] Started on port {_port}");
            Console.WriteLine($"[Server] Waiting for connections...");

            _ = Task.Run(() => HeartbeatCheckLoop(_cts.Token));

            while (!_cts.Token.IsCancellationRequested)
            {
                try
                {
                    var client = await _listener.AcceptTcpClientAsync();
                    _ = Task.Run(() => HandleClientAsync(client));
                }
                catch (Exception ex)
                {
                    Console.WriteLine($"[Server] Error accepting client: {ex.Message}");
                }
            }
        }

        private async Task HandleClientAsync(TcpClient client)
        {
            var session = new ClientSession(client);
            
            Console.WriteLine($"[Server] Client connected: {session.SessionId}");

            try
            {
                while (true)
                {
                    var packet = await session.ReceivePacketAsync();
                    
                    if (packet == null)
                    {
                        Console.WriteLine($"[Server] Client disconnected: {session.SessionId}");
                        break;
                    }

                    await ProcessPacketAsync(session, packet);
                }
            }
            catch (Exception ex)
            {
                Console.WriteLine($"[Server] Error handling client {session.SessionId}: {ex.Message}");
            }
            finally
            {
                RemoveSession(session);
                client.Close();
            }
        }

        private async Task ProcessPacketAsync(ClientSession session, GamePacket packet)
        {
            switch (packet.Type)
            {
                case PacketType.Login:
                    HandleLogin(session, packet);
                    break;

                case PacketType.Chat:
                    await HandleChatAsync(session, packet);
                    break;

                case PacketType.Move:
                    await BroadcastAsync(packet, session.SessionId);
                    break;

                case PacketType.Heartbeat:
                    session.LastHeartbeat = DateTime.UtcNow;
                    break;

                case PacketType.Logout:
                    Console.WriteLine($"[Server] Player {packet.PlayerId} logged out");
                    break;
            }
        }

        private void HandleLogin(ClientSession session, GamePacket packet)
        {
            session.PlayerId = packet.PlayerId;
            
            lock (_lock)
            {
                _sessions[session.SessionId] = session;
            }

            Console.WriteLine($"[Server] Player logged in: {packet.PlayerId} (Session: {session.SessionId})");
            Console.WriteLine($"[Server] Total players online: {_sessions.Count}");
        }

        private async Task HandleChatAsync(ClientSession session, GamePacket packet)
        {
            Console.WriteLine($"[Chat] {packet.PlayerId}: {packet.Data}");
            
            var broadcastPacket = new GamePacket
            {
                Type = PacketType.Chat,
                PlayerId = packet.PlayerId,
                Data = packet.Data,
                Timestamp = DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()
            };

            await BroadcastAsync(broadcastPacket);
        }

        private async Task BroadcastAsync(GamePacket packet, string? excludeSessionId = null)
        {
            List<ClientSession> sessions;
            
            lock (_lock)
            {
                sessions = _sessions.Values
                    .Where(s => s.SessionId != excludeSessionId)
                    .ToList();
            }

            foreach (var session in sessions)
            {
                try
                {
                    await session.SendPacketAsync(packet);
                }
                catch (Exception ex)
                {
                    Console.WriteLine($"[Server] Error broadcasting to {session.SessionId}: {ex.Message}");
                }
            }
        }

        private void RemoveSession(ClientSession session)
        {
            lock (_lock)
            {
                _sessions.Remove(session.SessionId);
            }
            
            Console.WriteLine($"[Server] Session removed: {session.SessionId}");
            Console.WriteLine($"[Server] Total players online: {_sessions.Count}");
        }

        private async Task HeartbeatCheckLoop(CancellationToken token)
        {
            while (!token.IsCancellationRequested)
            {
                await Task.Delay(10000, token); // 10초마다 체크

                var now = DateTime.UtcNow;
                var timeoutSessions = new List<ClientSession>();

                lock (_lock)
                {
                    timeoutSessions = _sessions.Values
                        .Where(s => (now - s.LastHeartbeat).TotalSeconds > 30)
                        .ToList();
                }

                foreach (var session in timeoutSessions)
                {
                    Console.WriteLine($"[Server] Heartbeat timeout: {session.SessionId}");
                    session.Client.Close();
                }
            }
        }

        public void Stop()
        {
            _cts?.Cancel();
            _listener?.Stop();
            
            lock (_lock)
            {
                foreach (var session in _sessions.Values)
                {
                    session.Client.Close();
                }
                _sessions.Clear();
            }
            
            Console.WriteLine("[Server] Stopped");
        }
    }
}
```

### Program.cs 수정

**Program.cs** 파일을 수정하여 서버를 실행한다.

```csharp
using GameSocketServer.Services;

var port = Environment.GetEnvironmentVariable("GAME_PORT") ?? "9000";
var server = new GameServer(int.Parse(port));

Console.CancelKeyPress += (sender, e) =>
{
    e.Cancel = true;
    Console.WriteLine("\n[Server] Shutting down...");
    server.Stop();
};

await server.StartAsync();
```

### 로컬 테스트

먼저 로컬에서 서버가 정상 동작하는지 확인한다.

```bash
dotnet run
```

서버가 시작되면 다음과 같은 출력을 볼 수 있다.

```
[Server] Started on port 9000
[Server] Waiting for connections...
```

## 5.2 소켓 서버용 Dockerfile 작성

소켓 서버를 Docker 컨테이너로 실행하기 위한 Dockerfile을 작성한다. REST API 서버와 기본 구조는 비슷하지만 포트 노출 방식에 차이가 있다.

### Dockerfile 작성

GameSocketServer 디렉토리에 **Dockerfile** 파일을 생성한다.

```dockerfile
# 빌드 스테이지
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build

WORKDIR /app

# 프로젝트 파일 복사 및 복원
COPY *.csproj ./
RUN dotnet restore

# 소스 코드 복사 및 빌드
COPY . ./
RUN dotnet publish -c Release -o out

# 런타임 스테이지
FROM mcr.microsoft.com/dotnet/runtime:8.0

WORKDIR /app

# 빌드 결과물 복사
COPY --from=build /app/out .

# 소켓 서버 포트 노출
EXPOSE 9000

# 환경 변수 설정
ENV GAME_PORT=9000

# 애플리케이션 실행
ENTRYPOINT ["dotnet", "GameSocketServer.dll"]
```

### REST API와의 차이점

소켓 서버 Dockerfile의 주요 차이점은 다음과 같다.

**베이스 이미지**: ASP.NET Runtime 대신 일반 .NET Runtime을 사용한다. 소켓 서버는 Kestrel 같은 웹 서버가 필요 없기 때문이다.

**포트 설정**: HTTP/HTTPS 포트 대신 커스텀 TCP 포트를 사용한다. 일반적으로 9000번대 포트를 사용한다.

**환경 변수**: ASPNETCORE 관련 변수 대신 게임 서버 전용 환경 변수를 사용한다.

### .dockerignore 파일

**.dockerignore** 파일을 생성하여 불필요한 파일을 제외한다.

```
bin/
obj/
*.user
.vs/
.vscode/
*.log
```

### 이미지 빌드

Dockerfile을 작성했으니 이미지를 빌드한다.

```bash
docker build -t socket-game-server:v1 .
```

빌드가 완료되면 이미지를 확인한다.

```bash
docker images | grep socket-game-server
```

## 5.3 네트워크 모드와 포트 설정

소켓 서버는 클라이언트와 지속적인 TCP 연결을 유지하므로 네트워크 설정이 중요하다. Docker의 다양한 네트워크 모드와 포트 설정 방법을 알아본다.

### Docker 네트워크 모드 개요

Docker는 여러 네트워크 모드를 제공한다.

```
┌──────────────────────────────────────────────┐
│         Docker Network Modes                 │
├──────────────────────────────────────────────┤
│                                              │
│  1. Bridge (기본값)                          │
│     - 컨테이너별 격리된 네트워크             │
│     - 포트 매핑으로 외부 접근                │
│                                              │
│  2. Host                                     │
│     - 호스트 네트워크 직접 사용              │
│     - 포트 매핑 불필요                       │
│     - 최고 성능                              │
│                                              │
│  3. None                                     │
│     - 네트워크 없음                          │
│     - 완전 격리                              │
│                                              │
│  4. Custom Bridge                            │
│     - 사용자 정의 네트워크                   │
│     - 컨테이너 간 이름 기반 통신             │
│                                              │
└──────────────────────────────────────────────┘
```

### 기본 Bridge 모드로 실행

가장 일반적인 방식으로 컨테이너를 실행한다.

```bash
docker run -d \
  -p 9000:9000 \
  --name game-socket-server \
  socket-game-server:v1
```

이 방식의 네트워크 구조는 다음과 같다.

```
┌─────────────────────────────────────────┐
│   Docker Host (WSL/Windows)             │
│                                         │
│   ┌─────────────────────────────────┐   │
│   │   docker0 bridge                │   │
│   │   172.17.0.1                    │   │
│   └──────────┬──────────────────────┘   │
│              │                          │
│   Port 9000  │                          │
│      ↓       │                          │
│   ┌──────────▼──────────────────────┐   │
│   │  Container                      │   │
│   │  socket-game-server             │   │
│   │  IP: 172.17.0.2                 │   │
│   │  Port: 9000                     │   │
│   └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
         ▲
         │ TCP Connection
    ┌────┴─────┐
    │  Client  │
    └──────────┘
```

### Host 모드로 실행 (리눅스 전용)

최고 성능이 필요한 경우 Host 모드를 사용한다. WSL에서는 사용 가능하지만 Windows Docker Desktop에서는 제한적이다.

```bash
docker run -d \
  --network host \
  --name game-socket-server-host \
  socket-game-server:v1
```

Host 모드에서는 포트 매핑이 불필요하며 컨테이너가 호스트의 네트워크를 직접 사용한다.

### 다중 포트 노출

여러 포트를 사용하는 게임 서버의 경우 여러 포트를 노출할 수 있다.

```bash
docker run -d \
  -p 9000:9000 \
  -p 9001:9001 \
  -p 9002:9002 \
  --name multi-port-server \
  socket-game-server:v1
```

### UDP 포트 노출

게임에서 빠른 업데이트를 위해 UDP를 사용하는 경우 다음과 같이 설정한다.

```bash
docker run -d \
  -p 9000:9000/tcp \
  -p 9001:9001/udp \
  --name game-server-mixed \
  socket-game-server:v1
```

### 포트 범위 매핑

많은 수의 연속된 포트가 필요한 경우 범위로 지정할 수 있다.

```bash
docker run -d \
  -p 9000-9010:9000-9010 \
  --name game-server-range \
  socket-game-server:v1
```

### 컨테이너 네트워크 정보 확인

실행 중인 컨테이너의 네트워크 정보를 확인한다.

```bash
docker inspect game-socket-server | grep -A 20 NetworkSettings
```

또는 더 간단하게 IP 주소만 확인한다.

```bash
docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' game-socket-server
```

## 5.4 클라이언트 연결 테스트

소켓 서버가 정상 동작하는지 확인하기 위해 테스트 클라이언트를 만든다.

### 간단한 테스트 클라이언트 작성

새 프로젝트를 생성하여 테스트 클라이언트를 만든다.

```bash
cd ..
dotnet new console -n GameClient
cd GameClient
```

**Program.cs** 파일을 다음과 같이 작성한다.

```csharp
using System.Net.Sockets;
using System.Text;
using System.Text.Json;

class GameClient
{
    private TcpClient? _client;
    private NetworkStream? _stream;
    private readonly string _host;
    private readonly int _port;
    private readonly string _playerId;

    public GameClient(string host, int port, string playerId)
    {
        _host = host;
        _port = port;
        _playerId = playerId;
    }

    public async Task ConnectAsync()
    {
        _client = new TcpClient();
        await _client.ConnectAsync(_host, _port);
        _stream = _client.GetStream();
        
        Console.WriteLine($"Connected to {_host}:{_port}");

        // 로그인 패킷 전송
        await SendLoginAsync();

        // 수신 루프 시작
        _ = Task.Run(ReceiveLoop);
    }

    private async Task SendLoginAsync()
    {
        var packet = new
        {
            Type = 1, // Login
            PlayerId = _playerId,
            Data = "",
            Timestamp = DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()
        };

        await SendPacketAsync(packet);
        Console.WriteLine($"Logged in as {_playerId}");
    }

    public async Task SendChatAsync(string message)
    {
        var packet = new
        {
            Type = 3, // Chat
            PlayerId = _playerId,
            Data = message,
            Timestamp = DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()
        };

        await SendPacketAsync(packet);
    }

    private async Task SendPacketAsync(object packet)
    {
        var json = JsonSerializer.Serialize(packet);
        var bytes = Encoding.UTF8.GetBytes(json);
        var length = BitConverter.GetBytes(bytes.Length);

        var data = new byte[4 + bytes.Length];
        Array.Copy(length, 0, data, 0, 4);
        Array.Copy(bytes, 0, data, 4, bytes.Length);

        await _stream!.WriteAsync(data, 0, data.Length);
    }

    private async Task ReceiveLoop()
    {
        try
        {
            while (true)
            {
                // 길이 읽기
                var lengthBuffer = new byte[4];
                await _stream!.ReadAsync(lengthBuffer, 0, 4);
                var length = BitConverter.ToInt32(lengthBuffer, 0);

                // 데이터 읽기
                var dataBuffer = new byte[length];
                var totalRead = 0;
                
                while (totalRead < length)
                {
                    var bytesRead = await _stream.ReadAsync(dataBuffer, totalRead, length - totalRead);
                    totalRead += bytesRead;
                }

                var json = Encoding.UTF8.GetString(dataBuffer);
                var packet = JsonSerializer.Deserialize<JsonElement>(json);
                
                var type = packet.GetProperty("Type").GetInt32();
                var playerId = packet.GetProperty("PlayerId").GetString();
                var data = packet.GetProperty("Data").GetString();

                if (type == 3) // Chat
                {
                    Console.WriteLine($"[{playerId}]: {data}");
                }
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Connection closed: {ex.Message}");
        }
    }

    public void Disconnect()
    {
        _stream?.Close();
        _client?.Close();
    }
}

class Program
{
    static async Task Main(string[] args)
    {
        var host = args.Length > 0 ? args[0] : "localhost";
        var port = args.Length > 1 ? int.Parse(args[1]) : 9000;
        var playerId = args.Length > 2 ? args[2] : $"Player_{Random.Shared.Next(1000, 9999)}";

        var client = new GameClient(host, port, playerId);
        
        try
        {
            await client.ConnectAsync();

            Console.WriteLine("Commands: /chat <message>, /quit");

            while (true)
            {
                var input = Console.ReadLine();
                
                if (string.IsNullOrEmpty(input)) continue;
                
                if (input == "/quit")
                {
                    break;
                }
                
                if (input.StartsWith("/chat "))
                {
                    var message = input.Substring(6);
                    await client.SendChatAsync(message);
                }
            }
        }
        finally
        {
            client.Disconnect();
        }
    }
}
```

### Docker 컨테이너 서버 테스트

먼저 서버 컨테이너가 실행 중인지 확인한다.

```bash
docker ps | grep game-socket-server
```

실행 중이 아니라면 시작한다.

```bash
docker run -d -p 9000:9000 --name game-socket-server socket-game-server:v1
```

서버 로그를 실시간으로 확인한다.

```bash
docker logs -f game-socket-server
```

### 클라이언트 실행 및 테스트

새 터미널을 열어 클라이언트를 실행한다.

```bash
cd GameClient
dotnet run localhost 9000 Player1
```

다른 터미널에서 두 번째 클라이언트를 실행한다.

```bash
dotnet run localhost 9000 Player2
```

첫 번째 클라이언트에서 메시지를 보낸다.

```
/chat Hello from Player1!
```

두 번째 클라이언트에서 메시지를 받았는지 확인한다.

### 연결 흐름 다이어그램

클라이언트-서버 간 통신 흐름은 다음과 같다.

```
Client 1              Server              Client 2
   │                     │                    │
   │   TCP Connect       │                    │
   ├────────────────────>│                    │
   │                     │                    │
   │   Login Packet      │                    │
   ├────────────────────>│                    │
   │                     │                    │
   │                     │   TCP Connect      │
   │                     │<───────────────────┤
   │                     │                    │
   │                     │   Login Packet     │
   │                     │<───────────────────┤
   │                     │                    │
   │   Chat Message      │                    │
   ├────────────────────>│                    │
   │                     │                    │
   │                     │   Broadcast        │
   │                     ├───────────────────>│
   │                     │                    │
   │   Heartbeat         │   Heartbeat        │
   ├────────────────────>│<───────────────────┤
   │                     │                    │
```

### telnet을 사용한 간단 테스트

복잡한 클라이언트 없이도 telnet으로 연결을 테스트할 수 있다.

```bash
telnet localhost 9000
```

연결이 성공하면 서버 로그에 새 클라이언트 연결 메시지가 나타난다.

### nc(netcat)를 사용한 테스트

telnet 대신 nc 명령어를 사용할 수도 있다.

```bash
nc localhost 9000
```

### 컨테이너 내부에서 포트 확인

컨테이너 내부에서 포트가 정상적으로 리슨하고 있는지 확인한다.

```bash
docker exec game-socket-server netstat -tlnp
```

또는 프로세스를 확인한다.

```bash
docker exec game-socket-server ps aux
```

### Windows에서 접속 테스트

Windows PowerShell에서도 접속을 테스트할 수 있다.

```powershell
Test-NetConnection -ComputerName localhost -Port 9000
```

### 다중 클라이언트 부하 테스트

간단한 스크립트로 여러 클라이언트를 동시에 연결한다.

```bash
# 10개의 클라이언트 동시 실행
for i in {1..10}; do
    dotnet run localhost 9000 "Player_$i" &
done
```

서버 로그에서 모든 연결을 확인한다.

```bash
docker logs game-socket-server
```

### 리소스 모니터링

서버의 리소스 사용량을 모니터링한다.

```bash
docker stats game-socket-server
```

실시간으로 CPU, 메모리, 네트워크 사용량을 확인할 수 있다.

### 문제 해결

연결이 안 될 때 체크할 사항들이다.

**포트가 정상적으로 매핑되었는지 확인**한다.

```bash
docker port game-socket-server
```

**방화벽이 포트를 차단하는지 확인**한다.

```bash
# Windows 방화벽 확인
netsh advfirewall firewall show rule name=all | grep 9000

# WSL에서 포트 리스닝 확인
sudo netstat -tlnp | grep 9000
```

**컨테이너 로그에서 에러를 확인**한다.

```bash
docker logs game-socket-server --tail 50
```

### 실습 체크리스트

이 장에서 배운 내용을 정리하면 다음과 같다.

□ TCP 소켓 기반 게임 서버를 구현했다  
□ 패킷 프로토콜과 직렬화 방식을 이해했다  
□ 소켓 서버용 Dockerfile을 작성했다  
□ Docker 네트워크 모드의 차이를 이해했다  
□ 포트 매핑 설정 방법을 익혔다  
□ 테스트 클라이언트로 연결을 검증했다  
□ 다중 클라이언트 환경을 테스트했다  
□ 컨테이너 네트워크 디버깅 방법을 배웠다

다음 장에서는 게임 데이터를 영구적으로 저장하기 위한 Docker 볼륨 사용법을 배운다. 컨테이너가 재시작되어도 플레이어 데이터가 유지되도록 하는 방법을 실습한다.  